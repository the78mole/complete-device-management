# RabbitMQ configuration for CDM Platform
#
# Serve the management UI under /rabbitmq/ so the nginx reverse proxy
# can route traffic to it without any path-stripping rewrite.
management.path_prefix = /rabbitmq
