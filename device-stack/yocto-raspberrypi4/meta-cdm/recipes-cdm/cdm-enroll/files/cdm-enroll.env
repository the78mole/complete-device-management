# /etc/cdm/enroll.env
#
# Static CDM enrollment configuration baked into the image.
# Individual values can be overridden at runtime via kernel cmdline:
#   cdm.device_id=rpi4-factory-001 cdm.tenant_id=tenant1 ...
#
# Append overrides to the LAST LINE of /boot/cmdline.txt on the SD card
# (before first boot, or via automated provisioning).

# [CHANGE ME] Unique device identifier (used as MQTT client ID and certificate CN)
DEVICE_ID=rpi4-device-001

# Device type tag (informational, sent to IoT Bridge API)
DEVICE_TYPE=rpi4

# [CHANGE ME] CDM tenant identifier
TENANT_ID=tenant1

# [CHANGE ME] Tenant IoT Bridge API base URL
BRIDGE_API_URL=http://192.168.1.100:8888/api

# [CHANGE ME] SHA-256 fingerprint of the Tenant Sub-CA certificate
# Obtain with: step certificate fingerprint <tenant-sub-ca.crt>
STEP_CA_FINGERPRINT=

# [CHANGE ME] ThingsBoard MQTT broker hostname/IP
THINGSBOARD_HOST=192.168.1.100

# ThingsBoard MQTT TLS port (default: 8883)
THINGSBOARD_MQTT_PORT=8883
